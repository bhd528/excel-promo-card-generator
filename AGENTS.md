## 全局偏好

- 默认使用简体中文与用户交流，除非用户明确要求其他语言。
- 代码、命令、文件路径、API 名称、错误码和专有名词保持原文。
- 遇到英文报错、日志或终端输出时，保留关键原文，并用中文解释含义和下一步。

## 项目维护约定

- 修改提取规则、页面行为、图片生成、运行方式或交付结构时，同步更新 `project_context_handoff.md`。
- 规则文档只维护 `rules/template_fill_rule.md`，不要再新增并行规则文件。
- GitHub 仓库不提交 Excel、生成图片、转换数据、构建产物、`.exe` 和 `.zip`。
